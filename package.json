{
  "name": "test-node",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "esbuild index.js --bundle --platform=node --target=node16 --packages=external --format=esm --outfile=out.js",
    "test": "npm run build && node out.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "esbuild": "0.17.14",
    "statsig-node": "^5.4.0"
  },
  "type": "module"
}
